import React from "react";
import Timer from "./Timer";

const Main = () => {
  return (
    <div>
      <main>
        <img src="{logo}" class="App-logo" alt="" />
      </main>
      <Timer />
    </div>
  );
};

export default Main;
